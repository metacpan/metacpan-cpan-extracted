requires 'MCP';
requires 'Mojolicious';          # Required by MCP::Server
requires 'Markdown::Perl';       # Pure Perl CommonMark spec
requires 'Path::Tiny';
requires 'Syntax::Keyword::Try';
requires 'JSON::MaybeXS';
requires 'Digest::SHA';

on test => sub {
  requires 'Test::More';
  requires 'File::Find::Rule';
};

on develop => sub {
  requires 'Dist::Zilla';
};