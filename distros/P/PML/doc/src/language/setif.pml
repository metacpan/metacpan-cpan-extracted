@include(../common/common.pmlh)
@SECTION(@CODE(setif))

The @CODE(setif) function is the same as the
@CODE(set) function except that the @CODE(setif)
function will not set the variable if it already
holds a value.
