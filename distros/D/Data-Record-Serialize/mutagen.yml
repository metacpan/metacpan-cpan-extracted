sync:
  defaults:
    flushOnCreate: true
    ignore:
      vcs: false
  code:
    alpha: "."
    beta: "macabre:/home/dj/axaf/src/Data-Record-Serialize"
    ignore:
      paths:
        - ".build"
        - "local"
        - "devel"
        - ".tidyall.d"
        - ".plx"
        - "mutagen.yml.lock"
