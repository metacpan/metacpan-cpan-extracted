# default server
server {
    listen                    *:80 default_server;
    listen                    [::]:80 default_server;
    server_name               "";
    return                    444;
}
