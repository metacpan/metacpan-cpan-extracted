use Test::More;
use FindBin qw($Bin);
use Digest::Oplop qw(oplop);

my $tests = do "$Bin/testdata.pl" or die "$!\n";

for my $test (@$tests) {
	is($test->{password},oplop($test->{master},$test->{label}),$test->{why});
}

done_testing();

__END__

test generated by json_xs -t dump
