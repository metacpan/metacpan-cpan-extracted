sync:
  defaults:
    flushOnCreate: true
    ignore:
      vcs: false
  code:
    alpha: "."
    beta: "macabre:/home/dj/axaf/src/p5-Qhull"
    ignore:
      paths:
        - ".build"
        - "local"
        - "devel"
        - ".tidyall.d"
        - ".plx"
        - "mutagen.yml.lock"
        - "caldb"
