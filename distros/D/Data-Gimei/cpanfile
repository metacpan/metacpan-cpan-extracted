requires 'perl', '5.010';

requires 'Class::Tiny';
requires 'File::Share';
requires 'YAML::XS';
requires 'version';

on configure => sub {
    requires 'Module::Build::Tiny';
};

on develop => sub {
    requires 'Perl::Tidy';
};

on test => sub {
    requires 'Test::More';
};
