Name:           perl-Package-Role-ini
Version:        0.07
Release:        1%{?dist}
Summary:        Perl role for "ini" object the returns a Config::IniFiles object
License:        perl
Group:          Development/Libraries
URL:            http://search.cpan.org/dist/Package-Role-ini/
Source0:        http://www.cpan.org/modules/by-module/Package/Package-Role-ini-%{version}.tar.gz
BuildArch:      noarch
BuildRequires:  perl(Config::IniFiles)
BuildRequires:  perl(ExtUtils::MakeMaker)
BuildRequires:  perl(Path::Class)
BuildRequires:  perl(Test::More)
BuildRequires:  perl(Package::New)
Requires:       perl(Config::IniFiles)
Requires:       perl(Path::Class)
Requires:       perl(:MODULE_COMPAT_%(eval "`%{__perl} -V:version`"; echo $version))

%description
Perl role for "ini" object that returns a Config::IniFiles object against a
default INI file name and location

%prep
%setup -q -n Package-Role-ini-%{version}

%build
%{__perl} Makefile.PL INSTALLDIRS=vendor
make %{?_smp_mflags}

%install
rm -rf $RPM_BUILD_ROOT

make pure_install PERL_INSTALL_ROOT=$RPM_BUILD_ROOT

find $RPM_BUILD_ROOT -type f -name .packlist -exec rm -f {} \;
find $RPM_BUILD_ROOT -depth -type d -exec rmdir {} 2>/dev/null \;

%{_fixperms} $RPM_BUILD_ROOT/*

%check
make test

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root,-)
%doc Changes META.json README
%{perl_vendorlib}/*
%{_mandir}/man3/*

%changelog
* Wed Oct 07 2020 Michael R. Davis <mrdvt92> 0.01-1
- Specfile autogenerated by cpanspec 1.78.
