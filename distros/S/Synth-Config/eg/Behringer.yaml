model: 'Behringer'
patches:
  - patch: 'Basic'
    settings:
      - group: 'MIDI-cv'
        parameters:
          - param : 'trig-out 1'
            control: 'patch'
            group_to: 'ENV 1'
            param_to: 'gate-in'
          - param : 'cv-out 1'
            control: 'patch'
            group_to: 'OSC 1'
            param_to: 'mod-in'
          - param : 'trig-out 2'
            control: 'patch'
            group_to: 'ENV 2'
            param_to: 'gate-in'
          - param : 'cv-out 2'
            control: 'patch'
            group_to: 'OSC 2'
            param_to: 'mod-in'
      - group: 'ENV 1'
        parameters:
          - param : 'env-out'
            control: 'patch'
            group_to: 'VCA 1'
            param_to: 'mod-in'
      - group: 'ENV 2'
        parameters:
          - param : 'env-out'
            control: 'patch'
            group_to: 'VCA 2'
            param_to: 'mod-in'
      - group: 'OSC 1'
        parameters:
          - param : 'out'
            control: 'patch'
            group_to: 'VCF 1'
            param_to: 'sig-in'
      - group: 'OSC 2'
        parameters:
          - param : 'out'
            control: 'patch'
            group_to: 'VCF 2'
            param_to: 'sig-in'
      - group: 'LFO'
        parameters:
          - param : 'out 1'
            control: 'patch'
            group_to: 'VCF 1'
            param_to: 'mod-in'
      - group: 'LFO'
        parameters:
          - param : 'out 2'
            control: 'patch'
            group_to: 'VCF 2'
            param_to: 'mod-in'
      - group: 'VCF 1'
        parameters:
          - param : 'out'
            control: 'patch'
            group_to: 'VCA 1'
            param_to: 'sig-in'
      - group: 'VCF 2'
        parameters:
          - param : 'out'
            control: 'patch'
            group_to: 'VCA 2'
            param_to: 'sig-in'
      - group: 'VCA 1'
        parameters:
          - param : 'out'
            control: 'patch'
            group_to: 'MIX'
            param_to: 'left in'
      - group: 'VCA 2'
        parameters:
          - param : 'out'
            control: 'patch'
            group_to: 'MIX'
            param_to: 'right in'
