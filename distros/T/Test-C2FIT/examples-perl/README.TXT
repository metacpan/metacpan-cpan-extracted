This directory is named examples-perl as it contains
examples specific to the perl-port only.

The directory examples contains examples which comes from
the reference implementation, but of course you can run
them under perl.


