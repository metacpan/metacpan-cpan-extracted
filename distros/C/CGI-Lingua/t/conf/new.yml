---
# Run: fuzz-harness-generator -r schemas/new.yml
config:
  dedup: 1
  test_empty: 0
  test_non_ascii: 0
  test_nuls: 0
  test_undef: 0

module: CGI::Lingua
function: new

input:
  supported:
    type: string
    min: 2
    max: 5

output:
  isa: CGI::Lingua
  type: object
