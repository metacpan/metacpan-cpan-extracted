#/usr/bin/!perl

use Test::More qw(no_plan);

use_ok(	'Config'				);	
use_ok(	'Crypt::Password'		);
use_ok(	'Exporter::Tiny'		);
use_ok(	'IO::Compress::Bzip2'	);
use_ok(	'Path::Tiny'			);
use_ok(	'Tie::Array::CSV'		);
use_ok(	'Passwd::Unix'			);


