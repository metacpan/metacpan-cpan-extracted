#!/usr/bin/env perl
# -*- cperl -*-
use strict;
$^W = 1;

use Acme::Rant;

my (@#%@-ing @list) = qw[foo];

print the $#@%-ing @list, "\n";

if ( $list[0] eq 'foo' ) {
	!*%^-you() and @#%-me ;
}

if ( @list < 2 ) {
	die what the @#$%, RANT {
		What the heck do you think you're doing?!
	};
}
