0.000028  2026-05-16 18:28:00-07:00 America/Los_Angeles

    - Persist suspend deadlines in SharedMem clients map under clients->{$id}{suspend_expires_at}; add pre_suspend_hook + peer_suspend_expires overrides; init() clears slot on fresh registration; bump IPC::Manager prereq to 0.000037
    - Add [PruneCruft] to dist.ini so blib/, pm_to_blib, MYMETA.* stop leaking into release tarballs
    - Fix SIGKILL-stale-peer leak in SharedMem clients map: init() reaps dead-pid entry on registration; peer_left() sweeps dead-pid entries + messages + stats; peers() filters dead-pid entries so peer_delta fires peer_left

0.000027  2026-04-25 01:49:50-07:00 America/Los_Angeles

    - Mirror the IPC-Manager test split: drop the aggregate t/SharedMem.t in favour of one .t file per subtest under t/SharedMem/, calling IPC::Manager::Test->run_one() so prove -j parallelises across subtests instead of running them serially. Bumps the IPC::Manager prereq to 0.000033 for run_one()

0.000026  2026-04-19 08:38:33-07:00 America/Los_Angeles

0.000001  2026-04-19 08:36:22-07:00 America/Los_Angeles

0.000001  2026-04-19

    - Initial release, split out from IPC::Manager 0.000026
