function love.conf(t)
    t.identity = "{{project_name}}"
    t.title = "{{project_name}}"

    t.version = "11.5"

    t.window.width = 1280
    t.window.height = 720
    t.window.resizable = true
end
