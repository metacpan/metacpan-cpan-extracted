This is Perl module HTTP::Response::Parser.

INSTALLATION

HTTP::Response::Parser installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan HTTP::Response::Parser

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

HTTP::Response::Parser documentation is available as in POD. So you can do:

    % perldoc HTTP::Response::Parser

to read the documentation online with your favorite pager.

mala
