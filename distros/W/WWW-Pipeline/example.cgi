#!/usr/bin/perl

use lib 'lib/';
use strict;
use warnings;


use WWW::Pipeline::Example;

#===============================================================================
(WWW::Pipeline::Example->new())->run();
exit(0);
