Name:           perl-Geo-GoogleEarth-Pluggable-Plugin-AsGeoJSON
Version:        0.05
Release:        1%{?dist}
Summary:        PostgreSQL ST_AsGeoJSON plugin for Geo::GoogleEarth::Pluggable
License:        mit
Group:          Development/Libraries
URL:            http://search.cpan.org/dist/Geo-GoogleEarth-Pluggable-Plugin-AsGeoJSON/
Source0:        http://www.cpan.org/modules/by-module/Geo/Geo-GoogleEarth-Pluggable-Plugin-AsGeoJSON-%{version}.tar.gz
BuildRoot:      %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)
BuildArch:      noarch
BuildRequires:  perl(ExtUtils::MakeMaker)
Requires:       perl(Geo::GoogleEarth::Pluggable) >= 0.16
Requires:       perl(JSON::XS)
BuildRequires:  perl(Geo::GoogleEarth::Pluggable) >= 0.16
BuildRequires:  perl(JSON::XS)
Requires:       perl(:MODULE_COMPAT_%(eval "`%{__perl} -V:version`"; echo $version))

%description
Parses the string as returned from the PostgreSQL ST_AsGeoJSON() function
as Google Earth compatible objects.

%prep
%setup -q -n Geo-GoogleEarth-Pluggable-Plugin-AsGeoJSON-%{version}

%build
%{__perl} Makefile.PL INSTALLDIRS=vendor
make %{?_smp_mflags}

%install
rm -rf $RPM_BUILD_ROOT

make pure_install PERL_INSTALL_ROOT=$RPM_BUILD_ROOT

find $RPM_BUILD_ROOT -type f -name .packlist -exec rm -f {} \;
find $RPM_BUILD_ROOT -depth -type d -exec rmdir {} 2>/dev/null \;

%{_fixperms} $RPM_BUILD_ROOT/*

%check
make test

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root,-)
%doc Changes README.md LICENSE
%{perl_vendorlib}/*
%{_mandir}/man3/*

%changelog
* Wed Jul 13 2016 Michael R. Davis <mrdvt@cpan.org> 0.01-1
- Specfile autogenerated by cpanspec 1.78.
