# Included code

Run `docbook-convert --pandoc guide.xml > guide.md` from this directory.
The text from example.txt appears in a fenced code block. Change example.txt
and rerun the command to update the generated guide.
