Revision history for the module "App::Bin4TSV" : 

0.100   2021-05-27T18:10+0900
        実験的に1個のモジュールをBUILD_REQUIRESに書き加えた。

0.101   2021-05-27T18:50+0900
        計7個のモジュールをBUILD_REQUIRESに書いた。
        ls -1d Bin* | perl -pe 's/^/"/;s/$/"/ ; s/$/ => 0 , /' | tr -d "\n"  を利用した。

0.102   2021-05-27T23:06+0900
        BUILD_REQUIRESでなくて、PREREQ_PMで設定していた恐れがあるので、改訂。
        cpanm --save-dist App::Bin4TSV でインストールするので、ファイル INSTALL を書き換え。 

0.320   2021-05-27T23:55+0900
        単にバージョンの番号を上げてみた。実験のため。

0.321   2021-05-28T00:05+0900
        .pm ファイルの package 名が間違っていたので訂正。

0.322   2021-05-28T16:05+0900
        再び BUILD_REQUIRES に必要な7個のモジュール名を記載。PREREQ_PM にも同じハッシュを書くようにした。

0.350   2021-05-29T02:55+09:00
        0.322 以前の方式は、cpanm でインストールしようにも、依存モジュールを自動的にはインストールしてくれないので、
	このモジュールから直接、50個超のプログラムをインストールするようにしてみた。
