name = "Art-World"
badges = ["metacpan", "gitlab-pipeline", "gitlab-coverage"]
authority="cpan:SMONFF"

module_maker="ModuleBuildTiny"
static_install = "auto"

# Can be set to false if an issue happen with those tests while releasing
[ReleaseTest]
MinimumVersion = false

#readme_from = "lib/Art/World.pm"

#[release]
#do_not_upload_to_cpan = false


