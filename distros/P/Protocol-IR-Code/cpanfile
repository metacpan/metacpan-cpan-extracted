requires 'perl', '5.010';

requires 'JSON::PP', '2.00';

# Used by the installed bin/ tools to fetch IRDB CSV files.
requires 'HTTP::Tiny', '0.017';

on 'test' => sub {
    requires 'Test::More', '0.88';
};
