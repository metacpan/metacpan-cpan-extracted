# autogenerated by /home/rlauer/bin/make-cpan-dist.pl on Sun Mar 15 11:04:27 2026

use strict;
use warnings;

use ExtUtils::MakeMaker;
use File::ShareDir::Install;

if ( -d 'share' ) {
  install_share 'share';
}

WriteMakefile(
  NAME             => 'Data::NestedKey',
  MIN_PERL_VERSION => '5.010',
  AUTHOR           => 'Rob Lauer <rlauer@treasurersbriefcase.com>',
  VERSION_FROM     => 'lib/Data/NestedKey.pm',
  ABSTRACT         => 'Data::NestedKey',
  LICENSE          => 'perl',
  PL_FILES         => {},
  EXE_FILES        => [],
  MAN3PODS         => {},
  PREREQ_PM        => {
    'JSON'       => '0',
    'List::Util' => '1.32',
    'YAML::XS'   => '0'
  },
  BUILD_REQUIRES => {
    'ExtUtils::MakeMaker'     => '6.64',
    'File::ShareDir::Install' => 0,
  },
  CONFIGURE_REQUIRES => {
    'ExtUtils::MakeMaker'     => '6.64',
    'File::ShareDir::Install' => 0,
  },
  TEST_REQUIRES => {
    'JSON'       => '0',
    'Storable'   => '0',
    'Test::More' => '0',
    'YAML::XS'   => '0'
  },
  META_MERGE => {
    'meta-spec' => { 'version' => 2 },
    'provides'  => {
      'Data::NestedKey' => {
        'file'    => 'lib/Data/NestedKey.pm',
        'version' => '1.0.8'
      }
    },
    'resources' => {
      'bugtracker' => {
        'mailto' => 'rlauer@treasurersbriefcase.com',
        'web'    => 'http://github.com/rlauer6/data-nestedkey/issues'
      },
      'homepage'   => 'http://github.com/rlauer6/data-nestedkey.git',
      'repository' => {
        'type' => 'git',
        'url'  => 'git://github.com/rlauer6/data-nestedkey.git',
        'web'  => 'http://github.com/rlauer6/data-nestedkey.git'
      }
    }
  }

);

package MY;

use File::ShareDir::Install;
use English qw(-no_match_vars);

sub postamble {
  my $self = shift;

  my @ret = File::ShareDir::Install::postamble($self);

  my $postamble = join "\n", @ret;

  if ( -e 'postamble' ) {

    local $RS = undef;

    open my $fh, '<', 'postamble'
      or die "could not open postamble\n";

    $postamble .= <$fh>;

    close $fh;
  }

  return $postamble;
}

1;
