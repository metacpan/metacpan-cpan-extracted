
  package OpenSearch::Parameters::URL::analyze_wildcard;
  use Moose::Role;

  has "analyze_wildcard" => (
    is => "rw",
    isa => "Bool",
    documentation => {
      encode_func => "encode_bool",
      required => undef,
    }
  );

  around "analyze_wildcard" => sub {
    my $orig = shift;
    my $self = shift;

    if(@_) {
      $self->$orig(@_);
      return($self);
    }
    return($self->$orig);
  };

1;
