You should be able to install the module...

cpanspec -b tarfile.tar.gz

OR

cpan2rpm tarfile.tar.gz

OR

perl Makefile.PL
make
make test
make install

If you are on a windows box you should use 'nmake' rather than 'make'.
