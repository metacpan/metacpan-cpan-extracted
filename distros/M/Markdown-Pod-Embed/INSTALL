# Installing Markdown::Pod::Embed

Install prerequisites and this distribution with `cpanm .`.
See README.md for external tools and the installation order of the related
documentation distributions. For a manual build, run `perl Makefile.PL`,
`make`, `make test`, and `make install`.
