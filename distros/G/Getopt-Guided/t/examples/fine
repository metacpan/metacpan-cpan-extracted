use strict;
use warnings;

$main::VERSION = 'v6.6.6';

use Getopt::Guided qw( print_version_info processopts );

sub run ( \@ ) {
  my @argv = @{ $_[ 0 ] };

  return 2 unless my $rv = processopts @argv, V => \&print_version_info, x => sub { };
  return 0 if $rv eq '-V';
  3
}

exit run @ARGV
