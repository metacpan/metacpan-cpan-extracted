define contact {
  contact_name  [% contact.name %]
  alias         [% contact.alias %]
  email         [% contact.email %]
  contactgroups [% contact.groups.join(',') %]
  use           generic-contact
}
