Name:           perl-Excel-Writer-XLSX-Simple-Tabs
Version:        0.03
Release:        1%{?dist}
Summary:        Simple Interface to the Excel::Writer::XLSX Package
License:        GPL+ or Artistic
Group:          Development/Libraries
URL:            http://search.cpan.org/dist/Excel-Writer-XLSX-Simple-Tabs/
Source0:        http://www.cpan.org/modules/by-module/Excel/Excel-Writer-XLSX-Simple-Tabs-%{version}.tar.gz
BuildRoot:      %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)
BuildArch:      noarch
BuildRequires:  perl(Excel::Writer::XLSX)
BuildRequires:  perl(ExtUtils::MakeMaker)
BuildRequires:  perl(ExtUtils::MakeMaker::META_MERGE::GitHub)
BuildRequires:  perl(IO::Scalar)
BuildRequires:  perl(Package::New)
BuildRequires:  perl(Test::Simple) >= 0.44
Requires:       perl(Excel::Writer::XLSX)
Requires:       perl(IO::Scalar)
Requires:       perl(Package::New)
Requires:       perl(:MODULE_COMPAT_%(eval "`%{__perl} -V:version`"; echo $version))

%description
This is a simple wrapper around Excel::Writer::XLSX that creates tabs for
data. It is meant to be simple not full featured. I use this package to
export data from the DBIx::Array sqlarrayarrayname method which is an array
of array references where the first array is the column headings.

%prep
%setup -q -n Excel-Writer-XLSX-Simple-Tabs-%{version}

%build
%{__perl} Makefile.PL INSTALLDIRS=vendor
make %{?_smp_mflags}

%install
rm -rf $RPM_BUILD_ROOT

make pure_install PERL_INSTALL_ROOT=$RPM_BUILD_ROOT

find $RPM_BUILD_ROOT -type f -name .packlist -exec rm -f {} \;
find $RPM_BUILD_ROOT -depth -type d -exec rmdir {} 2>/dev/null \;

%{_fixperms} $RPM_BUILD_ROOT/*

%check
make test

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root,-)
%doc Changes LICENSE
%{perl_vendorlib}/*
%{_mandir}/man3/*

%changelog
* Thu May 19 2022 Michael R. Davis <mrdvt@cpan.org> 0.02-1
- Specfile autogenerated by cpanspec 1.78.
