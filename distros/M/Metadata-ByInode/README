Metadata::ByInode
=================

Installing:

If you extracted files to /tmp/Metadata-ByInode
cd /tmp/Metadata-ByInode
perl Makefile.PL
make test;
make install;

TODO:
can't install with cpan because it fails on $cwd
