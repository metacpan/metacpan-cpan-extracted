1.02	2026-06-01

	(API) Add wifi-zone, wifi_zone_active, remove_wifi_zone.
	Update dependencies in Makefile.PL.
	Fix some doc typos.

1.01	2026-05-31

	(API) Add logout, kindex.
	(Tracker) Add has_feature, position_refresh.
	(Tracker) Add on/off argument to flash.
	(Tracker) Add get_zone, add_zone, remove_zone.
		get_wifizone, sos_call.
	get_*zones now return a collection object.
	get_trackers now return a collection object.
	Add Makefile.PL to cpan kit.

1.00	2026-05-30

	(Tracker) Add super_live.
	(User) Add get_animals.
	(User) Add get_preferences / set_preferences.
	Initial release.
