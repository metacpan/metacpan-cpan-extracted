
This is not built by default.  Use the Makefile.PL here to build
speedy.ph.  Then use speedy_dump.pl to dump out the contents of the
speedy temp file.

The missing_slots script will check for any missing slots in the output
from speedy_dump.pl

struct_sizes.pl shows the sizes of the slot structs
