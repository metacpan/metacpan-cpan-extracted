#!perl

use Test2::API qw/intercept run_subtest/;
use Test::More;

my $events = intercept {

    run_subtest('t',
                sub {
                    my $todo = todo(q{we'll get to this later});
                    fail('to be done');
                }, 1);

};

use Data::Dumper;
print STDERR Dumper($events);
