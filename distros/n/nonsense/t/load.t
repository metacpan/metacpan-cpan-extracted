use strict;
use warnings;
use Test::More;

use ok 't::load';
# use ok 't::typelibrary', 'FortyTwo';
# use ok 't::class';
# use ok 't::library', 'fortytwo';

# ok(t::library->can('import'), 'library can import');
# ok(t::class->can('foo'), 'has worked');
# ok(t::class->can('bar'), 'role worked');
# ok(!t::class->can('has'), 'cleaner worked');
# is(fortytwo(), 42, 'got 42');
# ok FortyTwo(), 'got type';
done_testing;
