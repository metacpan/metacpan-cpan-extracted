# autogenerated by /home/rlauer/bin/make-cpan-dist.pl on Sun Dec  7 11:15:44 2025

use strict;
use warnings;

use ExtUtils::MakeMaker;
use File::ShareDir::Install;

if ( -d 'share' ) {
  install_share 'share';
}

WriteMakefile(
  NAME             => 'Config::Resolver',
  MIN_PERL_VERSION => '5.010',
  AUTHOR           => 'Rob Lauer <rclauer@gmail.com>',
  VERSION_FROM     => 'lib/Config/Resolver.pm',
  ABSTRACT         => 'Config::Resolver',
  LICENSE          => 'perl',
  PL_FILES         => {},
  EXE_FILES        => ['bin/config-resolver.pl'],
  PREREQ_PM        => {
    'CLI::Simple'           => '0',
    'Class::Accessor::Fast' => '0',
    'Config::INI::Tiny'     => '0',
    'JSON'                  => '0',
    'List::Util'            => '0',
    'Module::Load'          => '0',
    'Query::Param'          => '0',
    'Readonly'              => '0',
    'YAML::Tiny'            => '0'
  },
  BUILD_REQUIRES => {
    'ExtUtils::MakeMaker'     => '6.64',
    'File::ShareDir::Install' => 0,
  },
  CONFIGURE_REQUIRES => {
    'ExtUtils::MakeMaker'     => '6.64',
    'File::ShareDir::Install' => 0,
  },
  TEST_REQUIRES => {
    'IO::Scalar' => '0',
    'IO::String' => '0',
    'JSON'       => '0',
    'Test::Warn' => '0',
    'YAML::Tiny' => '0'
  },
  META_MERGE => {
    'meta-spec' => { 'version' => 2 },
    'provides'  => {
      'Config::Resolver' => {
        'file'    => 'lib/Config/Resolver.pm',
        'version' => '1.0.10'
      },
      'Config::Resolver::Utils' => {
        'file'    => 'lib/Config/Resolver/Utils.pm',
        'version' => '1.0.10'
      }
    },
    'resources' => {
      'bugtracker' => {
        'mailto' => 'rclauer@gmail.com',
        'web'    => 'http://github.com/rlauer6/Config-Resolver/issues'
      },
      'homepage'   => 'http://github.com/rlauer6/Config-Resolver.git',
      'repository' => {
        'type' => 'git',
        'url'  => 'git://github.com/rlauer6/Config-Resolver.git',
        'web'  => 'http://github.com/rlauer6/Config-Resolver'
      }
    }
  }

);

package MY;

use File::ShareDir::Install;
use English qw(-no_match_vars);

sub postamble {
  my $self = shift;

  my @ret = File::ShareDir::Install::postamble($self);

  my $postamble = join "\n", @ret;

  if ( -e 'postamble' ) {

    local $RS = undef;

    open my $fh, '<', 'postamble'
      or die "could not open postamble\n";

    $postamble .= <$fh>;

    close $fh;
  }

  return $postamble;
}

1;
