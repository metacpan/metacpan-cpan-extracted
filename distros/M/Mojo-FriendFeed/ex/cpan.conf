{
  # nickname => 'release_bot',
  # server   => 'irc.perl.org:6667',
  # user     => 'new cpan releases',
  jobs     => [
    {
      channel => '#release',
      # dist => qr//,
      # deps => qr//,
    },
  ],
}

