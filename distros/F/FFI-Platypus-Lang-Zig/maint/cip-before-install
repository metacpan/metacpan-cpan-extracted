#!/bin/bash

set -ex

cip exec bash -c "cd ~ && curl -L https://ziglang.org/download/0.15.1/zig-x86_64-linux-$ZIG_VERSION.tar.xz | xz -d | tar xf -"
cip exec bash -c "mv ~/zig-x86_64-linux-$ZIG_VERSION ~/bin"
