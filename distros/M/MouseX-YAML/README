This is Perl module MouseX::YAML.

INSTALLATION

MouseX::YAML installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    $ cpan MouseX::YAML

Download it, unpack it, then build it as per the usual:

    $ perl Makefile.PL
    $ make && make test

Then install it:

    $ make install

DOCUMENTATION

MouseX::YAML documentation is available as in POD. So you can do:

    $ perldoc MouseX::YAML

to read the documentation online with your favorite pager.

Goro Fuji (gfx)
