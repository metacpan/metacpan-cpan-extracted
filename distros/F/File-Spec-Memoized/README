This is Perl module File::Spec::Memoized.

INSTALLATION

File::Spec::Memoized installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    $ cpan File::Spec::Memoized

Download it, unpack it, then build it as per the usual:

    $ perl Makefile.PL
    $ make && make test

Then install it:

    $ make install

DOCUMENTATION

File::Spec::Memoized documentation is available as in POD. So you can do:

    $ perldoc File::Spec::Memoized

to read the documentation online with your favorite pager.

Goro Fuji (gfx)
