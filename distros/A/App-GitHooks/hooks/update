#!/usr/bin/env perl

use strict;
use warnings;

use App::GitHooks;


=head1 NAME

update - Call App::GitHooks for the update hook.

=cut

App::GitHooks->run(
    name      => $0,
    arguments => \@ARGV,
);
