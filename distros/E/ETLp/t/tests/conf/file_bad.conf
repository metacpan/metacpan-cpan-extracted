<test>
	type = serial
	
	# Can be left out if not called
	<pre_process />
	<process>
		<item>
			name     = dummy
			type     = plsql
			proc     = dummy.pproc
			on_error = ignore
		</item>
	# Uh-oh! Tag Not Closed!!!!
	</process

</test>