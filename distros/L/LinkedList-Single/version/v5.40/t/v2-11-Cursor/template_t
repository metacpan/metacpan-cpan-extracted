########################################################################
# housekeeping
########################################################################
package LinkedList::Single::Testy;
use v5.40;
use FindBin::libs;

use Test::More;
use LinkedList::Single::TestUtil;

########################################################################
# tests
########################################################################

my $method  = $test->method;
my @data    = $test->gen_expect;

my $prep
= sub( $list_c )
{
    my $list    = $list_c->new->append( @data );

    ( $list, $list->cursor )
};

my $pass1
= sub( $curs )
{
    $list->next_node
};

my $pass2
= sub( $curs )
{
    $curs->node
};

$test->generic_cursor( $prep, $pass1, $pass2 );
done_testing
__END__
