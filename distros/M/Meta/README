Meta is a class library of about 250 classes. The idea is to provide a full class library to accomplish many tasks. Some applications which make use of the class library are also provided. This package is pre-alpha material at best and the package has many dependencies which makes it a bit harder to install.