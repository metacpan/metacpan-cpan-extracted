% my $a = shift;
%% my $title = 'New record in table "<%= $a->{t} %>"';
%% layout 'blog', title => $title;
<h2><%%= $title %></h2>
%%= include '<%= $a->{t} %>/_form', caption => 'Create', target => 'store_<%= $a->{t} %>'
