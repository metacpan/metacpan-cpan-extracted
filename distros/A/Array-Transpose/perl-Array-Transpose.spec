Name:           perl-Array-Transpose
Version:        0.06
Release:        1%{?dist}
Summary:        Transposes a 2-Dimensional Array
License:        BSD
Group:          Development/Libraries
URL:            http://search.cpan.org/dist/Array-Transpose/
Source0:        http://www.cpan.org/modules/by-module/Array/Array-Transpose-%{version}.tar.gz
BuildRoot:      %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)
BuildArch:      noarch
BuildRequires:  perl(ExtUtils::MakeMaker)
BuildRequires:  perl(Test::Simple) >= 0.44
Requires:       perl(:MODULE_COMPAT_%(eval "`%{__perl} -V:version`"; echo $version))

%description
This package exports one function named transpose.

%prep
%setup -q -n Array-Transpose-%{version}

%build
%{__perl} Makefile.PL INSTALLDIRS=vendor
make %{?_smp_mflags}

%install
rm -rf $RPM_BUILD_ROOT

make pure_install PERL_INSTALL_ROOT=$RPM_BUILD_ROOT

find $RPM_BUILD_ROOT -type f -name .packlist -exec rm -f {} \;
find $RPM_BUILD_ROOT -depth -type d -exec rmdir {} 2>/dev/null \;

%{_fixperms} $RPM_BUILD_ROOT/*

%check
make test

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root,-)
%doc Changes LICENSE README Todo
%{perl_vendorlib}/*
%{_mandir}/man3/*

%changelog
* Thu Nov 04 2010 Michael R. Davis (mdavis@stopllc.com) 0.05-1
- Specfile autogenerated by cpanspec 1.78.
