CREATE TABLE a (
  id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
  b_id INTEGER
);

CREATE TABLE b (
  id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
  someval INTEGER NOT NULL
);

INSERT INTO a VALUES(NULL,NULL);
INSERT INTO a VALUES(NULL,2);

INSERT INTO b VALUES(NULL,111);
INSERT INTO b VALUES(NULL,222);
