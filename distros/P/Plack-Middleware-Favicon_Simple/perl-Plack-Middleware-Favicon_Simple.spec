Name:           perl-Plack-Middleware-Favicon_Simple
Version:        0.01
Release:        1%{?dist}
Summary:        Perl Plack Middleware to provide favicon
License:        MIT
Group:          Development/Libraries
URL:            http://search.cpan.org/dist/Plack-Middleware-Favicon_Simple/
Source0:        http://www.cpan.org/modules/by-module/Plack/Plack-Middleware-Favicon_Simple-%{version}.tar.gz
BuildRoot:      %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)
BuildArch:      noarch
BuildRequires:  perl(ExtUtils::MakeMaker)
BuildRequires:  perl(Plack::Middleware)
Requires:       perl(Plack::Middleware)
Requires:       perl(:MODULE_COMPAT_%(eval "`%{__perl} -V:version`"; echo $version))

%description
Browsers request /favicon.ico automatically. This Plack Middleware returns
a favicon.ico file so that browsers do not get 404 HTTP codes.

%prep
%setup -q -n Plack-Middleware-Favicon_Simple-%{version}

%build
%{__perl} Makefile.PL INSTALLDIRS=vendor
make %{?_smp_mflags}

%install
rm -rf $RPM_BUILD_ROOT

make pure_install PERL_INSTALL_ROOT=$RPM_BUILD_ROOT

find $RPM_BUILD_ROOT -type f -name .packlist -exec rm -f {} \;
find $RPM_BUILD_ROOT -depth -type d -exec rmdir {} 2>/dev/null \;

%{_fixperms} $RPM_BUILD_ROOT/*

%check
make test

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root,-)
%doc Changes META.json README.md
%{perl_vendorlib}/*
%{_mandir}/man3/*

%changelog
* Sun Dec 25 2022 Michael R. Davis <mrdvt92@yahoo.com> 0.01-1
- Specfile autogenerated by cpanspec 1.78.
