#!/usr/bin/env perl

use strict;
use warnings;

use App::GitHooks;


=head1 NAME

pre-applypatch - Call App::GitHooks for the pre-applypatch hook.

=cut

App::GitHooks->run(
    name      => $0,
    arguments => \@ARGV,
);
