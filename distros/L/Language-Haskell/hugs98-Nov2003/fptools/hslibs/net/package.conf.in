Package {
	name		= "net",
#ifdef INSTALLING
	import_dirs	= [ "$libdir/hslibs-imports/net" ],
#else
	import_dirs	= [ "$libdir/hslibs/net" ],
#endif
	source_dirs	= [],
#ifdef INSTALLING
	library_dirs	= [ "$libdir" ],
#else
	library_dirs	= [ "$libdir/hslibs/net" ],
#endif
	hs_libraries	= [ "HSnet" ],
	extra_libraries	= [],
	include_dirs	= [],
	c_includes	= [],
	package_deps	= [ "network" ],
	extra_ghc_opts	= [],
	extra_cc_opts	= [],
	extra_ld_opts	= []
}
