Name:           perl-Array-Intersection
Version:        0.03
Release:        1%{?dist}
Summary:        Exports an array intersection function
License:        MIT
Group:          Development/Libraries
URL:            http://search.cpan.org/dist/Array-Intersection/
Source0:        http://www.cpan.org/modules/by-module/Array/Array-Intersection-%{version}.tar.gz
BuildRoot:      %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)
BuildArch:      noarch
BuildRequires:  perl(Exporter)
BuildRequires:  perl(ExtUtils::MakeMaker)
BuildRequires:  perl(List::MoreUtils)
Requires:       perl(Exporter)
Requires:       perl(List::MoreUtils)
Requires:       perl(:MODULE_COMPAT_%(eval "`%{__perl} -V:version`"; echo $version))

%description
This package exports the intersection() function

%prep
%setup -q -n Array-Intersection-%{version}

%build
%{__perl} Makefile.PL INSTALLDIRS=vendor
make %{?_smp_mflags}

%install
rm -rf $RPM_BUILD_ROOT

make pure_install PERL_INSTALL_ROOT=$RPM_BUILD_ROOT

find $RPM_BUILD_ROOT -type f -name .packlist -exec rm -f {} \;
find $RPM_BUILD_ROOT -depth -type d -exec rmdir {} 2>/dev/null \;

%{_fixperms} $RPM_BUILD_ROOT/*

%check
make test

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root,-)
%doc Changes META.json README.md
%{perl_vendorlib}/*
%{_mandir}/man3/*

%changelog
* Wed Nov 20 2024 Michael R. Davis <michael.davis@fcc.gov> 0.01-1
- Specfile autogenerated by cpanspec 1.78.
