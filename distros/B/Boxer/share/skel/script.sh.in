#!/bin/sh
# This is an install/update script.
# It was generated by Boxer with node "[% node %]"

set -e

# Workaround for dictionaries-common install failure (bug#751367)
apt-get install apt-utils
[% IF pkglist %]
[% pkgdesc %]
apt-get install [% pkglist %][% END %]

[% tweakdesc %]
suite=[% suite %]
[% IF pkgautolist %]
 apt-mark auto \
  [% pkgautolist %] || true
[% END %]
 [% tweaklist %]
