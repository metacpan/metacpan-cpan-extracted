TODO

 * Make & config perltidy work automatically
 * Meaningful tests
 * Minimal Perl version if not set
 * Validate that the most recent version of the plugin is installed
 * Combine both Test::EOL and Test::TrailingSpace, and run only for
   author.
 * AUTHOR_PLEDGE should not refer to Joelle directly
 * Add critic tests ([Test::Perl::Critic])
 * Allow parameters for the AutoPrereq plugin (see File::ByLine for use
   case)
 * Allow major version number to be passed (see Parallel::WorkUnit for
   use case)

