# -*- Mode: Perl; indent-tabs-mode: nil; -*-

use strict;
use warnings;

use ExtUtils::MakeMaker;

WriteMakefile(
              NAME         => 'Bundle::libservlet',
              VERSION_FROM => 'libservlet.pm',
             );
