======================================================================
 Helaian Rujukan Perl7::Handy                       [BM] Bahasa Melayu
======================================================================

[ 1. Installation ]

  Pasang:
    cpan Perl7::Handy

  Gunakan dalam skrip:
    use Perl7::Handy;

  Kesan (Perl 5.010001 ke atas):
    * use strict               -- memaksa pengisytiharan pemboleh ubah
    * use warnings             -- mengaktifkan amaran
    * no bareword::filehandles -- menolak gaya open(FILE,...)
    * no multidimensional      -- menyahaktifkan emulasi $hash{a,b}
    * use feature qw(signatures)  (Perl 5.020+)
    * no feature qw(indirect)     (Perl 5.031009+)

  Kesan (Perl 5.005_03 -- 5.008):
    * use strict
    * open/opendir/sysopen/pipe/socket/accept dengan autovivification
    # socket() tidak autodie semasa gagal (mengembalikan false dalam void context)
    * perangkap $; berasaskan tie (mencegah emulasi tatasusunan berbilang dimensi)

[ 2. open() -- autovivification ]

  my $fh;
  open($fh, "< file.txt");   # baca
  open($fh, "> file.txt");   # tulis (ganti)
  open($fh, ">> file.txt");  # tambah
  open($fh, "+< file.txt");  # baca/tulis
  while (my $line = readline($fh)) { ... }
  print $fh "text\n";
  close($fh);

  # pemegang bareword ditolak:
  open(FILE, "< file.txt");   # dies: Use of bareword handle in open

[ 3. open() -- 3-arg ]

  my $fh;
  open($fh, '<',  "file.txt");   # baca
  open($fh, '>',  "file.txt");   # tulis (ganti)
  open($fh, '>>', "file.txt");   # tambah
  open($fh, '+<', "file.txt");   # baca/tulis
  open($fh, '+>', "file.txt");   # baca/tulis (ganti)
  open($fh, '-|', "cmd");        # baca dari paip arahan
  open($fh, '|-', "cmd");        # tulis ke paip arahan

[ 4. opendir() / sysopen() ]

  my $dh;
  opendir($dh, "/path/to/dir");
  while (my $e = readdir($dh)) {
      next if $e eq '.' or $e eq '..';
      print "$e\n";
  }
  closedir($dh);

  use Fcntl qw(O_RDONLY O_WRONLY O_CREAT O_TRUNC);
  my $fh;
  sysopen($fh, "file.txt", O_RDONLY);
  sysopen($fh, "file.txt", O_WRONLY | O_CREAT | O_TRUNC, 0644);

[ 5. pipe() ]

  my($reader, $writer);
  pipe($reader, $writer);
  if (my $pid = fork()) {
      close($writer);
      while (my $line = readline($reader)) { print $line }
      close($reader);
  } else {
      close($reader);
      print $writer "Hai dari proses anak\n";
      close($writer);
      exit 0;
  }

[ 6. socket() / accept() ]

  use Socket qw(AF_INET SOCK_STREAM sockaddr_in inet_aton);
  my $server;
  socket($server, AF_INET, SOCK_STREAM, 0);
  # Nota: socket() tidak autodie semasa gagal; semak nilai pulangan secara eksplisit.
  my $client;
  accept($client, $server);

[ 7. no bareword::filehandles ]

  # pemegang bareword ditolak:
  open(FILE, "< file.txt");    # error: Bareword filehandle
  print FILE "hello\n";        # error: Bareword filehandle

  # gunakan pemboleh ubah leksikal:
  my $fh;
  open($fh, "< file.txt");
  print $fh "hello\n";
  close($fh);

[ 8. no multidimensional ]

  # emulasi hash pelbagai dimensi dinyahaktifkan:
  my %h;
  $h{1,2} = "val";    # error: Use of multidimensional array emulation

  # gunakan kunci eksplisit:
  $h{"$x,$y"} = "val";

[ 9. Signatures (Perl 5.020+) ]

  # tandatangan diaktifkan secara automatik; amaran ujikaji ditindas
  use Perl7::Handy;

  sub greet($name) { return "Hello, $name!" }
  sub add($x, $y)  { return $x + $y }

[ 10. no indirect (Perl 5.031009+) ]

  # sintaks objek tidak langsung dinyahaktifkan:
  my $obj = new MyClass;   # error: indirect syntax

  # gunakan sintaks langsung:
  my $obj = MyClass->new;

[ 11. CVE-2016-1238 ]

  # Perl7::Handy mengeluarkan direktori semasa dari @INC semasa permulaan.
  # BEGIN { pop @INC if $INC[-1] eq '.' }

[ 12. Matriks keserasian versi ]

  Feature              5.005_03  5.006-009  5.010+  5.020+  5.031009+
  -------------------  --------  ---------  ------  ------  ---------
  strict               yes       yes        yes     yes     yes
  warnings             no        yes        yes     yes     yes
  autovivif. open()    yes       yes        yes*    yes*    yes*
  no bareword FH       emulated  emulated   yes     yes     yes
  no multidim.         tie       tie        yes     yes     yes
  signatures           no        no         no      yes     yes
  no indirect          no        no         no      no      yes

[ 13. Official resources ]

  Perl7::Handy (MetaCPAN):
    https://metacpan.org/dist/Perl7-Handy

  bareword::filehandles:
    https://metacpan.org/dist/bareword-filehandles

  INABA Hitoshi (ina) on CPAN:
    https://metacpan.org/author/INA

======================================================================
