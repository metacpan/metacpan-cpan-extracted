
This is the Perl distribution PerlIO-via-ToFirePHP.

## Installation

PerlIO-via-ToFirePHP installation is straightforward.
If your CPAN shell is set up, you should just be able to do

    % cpan PerlIO::via::ToFirePHP

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

## Documentation

PerlIO-via-ToFirePHP documentation is available as in POD.
So you can do:

    % perldoc PerlIO::via::ToFirePHP

to read the documentation with your favorite pager.
