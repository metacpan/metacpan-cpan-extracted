<!--#perl 
  my ($vbObj,$status);
  $vbObj = &VBTK::Objects::FindObjByName($_parms->{name});
  
  $status = $vbObj->getStatus() if (defined $vbObj);
  
  $status = "Error" if (! defined $status);
  
-->$status