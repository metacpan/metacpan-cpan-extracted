sync:
  defaults:
    mode: "one-way-safe"
    flushOnCreate: true
    ignore:
      vcs: false
  code:
    alpha: "."
    beta: "macabre:/home/dj/axaf/src/Data-Record-Serialize-Encode-dbi"
    ignore:
      paths:
        - ".build"
        - "local"
        - "devel"
        - ".tidyall.d"
        - ".plx"
        - "mutagen.yml.lock"
