Name:           perl-GPS-Point
Version:        0.20
Release:        1%{?dist}
Summary:        Provides an object interface for a GPS point
License:        BSD
Group:          Development/Libraries
URL:            http://search.cpan.org/dist/GPS-Point/
Source0:        http://www.cpan.org/modules/by-module/GPS/GPS-Point-%{version}.tar.gz
BuildRoot:      %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)
BuildArch:      noarch
BuildRequires:  perl(ExtUtils::MakeMaker)
BuildRequires:  perl(Test::Number::Delta)
BuildRequires:  perl(Test::Simple) >= 0.44
Requires:       perl(:MODULE_COMPAT_%(eval "`%{__perl} -V:version`"; echo $version))

%description
This is a re-write of Net::GPSD::Point with the goal of being more re-usable.

%prep
%setup -q -n GPS-Point-%{version}

%build
%{__perl} Makefile.PL INSTALLDIRS=vendor
make %{?_smp_mflags}

%install
rm -rf $RPM_BUILD_ROOT

make pure_install PERL_INSTALL_ROOT=$RPM_BUILD_ROOT

find $RPM_BUILD_ROOT -type f -name .packlist -exec rm -f {} \;
find $RPM_BUILD_ROOT -depth -type d -exec rmdir {} 2>/dev/null \;

%{_fixperms} $RPM_BUILD_ROOT/*

%check
make test

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root,-)
%doc Changes LICENSE README Todo
%{perl_vendorlib}/*
%{_mandir}/man3/*

%changelog
* Thu May 05 2011 Michael R. Davis (mdavis@stopllc.com) 0.18-1
- Specfile autogenerated by cpanspec 1.78.
