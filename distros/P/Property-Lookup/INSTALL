
This is the Perl distribution Property-Lookup.

## Installation

Property-Lookup installation is straightforward.
If your CPAN shell is set up, you should just be able to do

    % cpan Property::Lookup

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

## Documentation

Property-Lookup documentation is available as in POD.
So you can do:

    % perldoc Property::Lookup

to read the documentation with your favorite pager.
