#!/usr/bin/env perl

#  PODNAME: app
# ABSTRACT: Executable to {{$name}}

use v5.14;
use strict;
use warnings;

use FindBin;

use lib "$FindBin::Bin/../lib";

use {{$name}};

my $app = {{$name}}->new;

$app->run;
