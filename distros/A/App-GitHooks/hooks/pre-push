#!/usr/bin/env perl

use strict;
use warnings;

use App::GitHooks;


=head1 NAME

pre-push - Call App::GitHooks for the pre-push hook.

=cut

App::GitHooks->run(
    name      => $0,
    arguments => \@ARGV,
);
