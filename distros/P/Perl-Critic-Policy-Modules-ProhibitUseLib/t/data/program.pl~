package module;

# these are not allowed
use lib qw(/some/where);
use lib '/some/where/else';
use lib q{/some/where/else};
use lib qq{/some/where/else};

# some day someone will try this ... and it is allowed
use library '/some/where/else';

