#!/usr/bin/env perl

use strict;
use warnings;

use App::GitHooks;


=head1 NAME

pre-rebase - Call App::GitHooks for the pre-rebase hook.

=cut

App::GitHooks->run(
    name      => $0,
    arguments => \@ARGV,
);
