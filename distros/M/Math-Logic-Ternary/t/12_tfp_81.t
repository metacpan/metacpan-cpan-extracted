# Copyright (c) 2014-2017 Martin Becker.  All rights reserved.
# This package is free software; you can redistribute it and/or modify it
# under the same terms as Perl itself.

# Tests for of Math::Logic::Ternary::TFP_81

# Before `make install' is performed this script should be runnable with
# `make test'. After `make install' it should work as `perl t/12_tfp_81.t'

use strict;
use Test::More tests => 1;
use Math::Logic::Ternary::Word;
use Math::Logic::Ternary::TFP_81 qw(float81);

pass;                                   # 1

# my $mantissa = Math::Logic::Ternary::Word->from_int(72, ...);
# my $exponent  = Math::Logic::Ternary::Word->from_int(9, ...);

__END__
