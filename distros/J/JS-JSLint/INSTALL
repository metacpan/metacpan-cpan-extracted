JS-JSLint Installation

# install via cpanminus
cpanm JS::JSLint

# install via CPAN shell
cpan JS::JSLint

# install manually
perl Build.PL
./Build
./Build test
./Build install

# locate on filesystem
js-cpan jslint
