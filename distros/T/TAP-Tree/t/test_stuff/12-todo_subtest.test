use strict;
use warnings;

use Test::More tests => 2;

ok( 1, 'first test' );

subtest 'second test' => sub {
    plan tests => 1;

    TODO: {
              local $TODO = 'Not implemented yet';
              ok( 0 );

          };
};
