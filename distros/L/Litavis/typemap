TYPEMAP
Litavis	T_PTROBJ

OUTPUT
T_PTROBJ
	sv_setref_pv($arg, \"${ntype}\", (void*)$var);

INPUT
T_PTROBJ
	if (sv_isobject($arg) && sv_derived_from($arg, \"${ntype}\"))
	    $var = INT2PTR($type, SvIV((SV*)SvRV($arg)));
	else
	    croak(\"$var is not of type ${ntype}\");
