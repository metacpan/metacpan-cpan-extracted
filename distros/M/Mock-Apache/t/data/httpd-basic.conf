# Basic Apache configuration file for testing

# Directories are in an unusual place so that they are not mistaken for real values

ServerRoot "/opt/mock-apache/httpd"
User       mock-apache
Group      mock-apache

