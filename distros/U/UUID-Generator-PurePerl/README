This is Perl module UUID::Generator::PurePerl.

INSTALLATION

UUID::Generator::PurePerl installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan UUID::Generator::PurePerl

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

UUID::Generator::PurePerl documentation is available as in POD. So you can do:

    % perldoc UUID::Generator::PurePerl

to read the documentation online with your favorite pager.

ITO Nobuaki (banb@cpan.org)
