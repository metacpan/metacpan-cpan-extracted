This is Perl module Acme::RequireModule.

INSTALLATION

Acme::RequireModule installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    $ cpan Acme::RequireModule

Download it, unpack it, then build it as per the usual:

    $ perl Makefile.PL
    $ make && make test

Then install it:

    $ make install

DOCUMENTATION

Acme::RequireModule documentation is available as in POD. So you can do:

    $ perldoc Acme::RequireModule

to read the documentation online with your favorite pager.

Goro Fuji
