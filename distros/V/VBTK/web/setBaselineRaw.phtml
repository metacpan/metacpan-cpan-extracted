<!--#perl 
  my $vbObj = &VBTK::Objects::FindObjByName($_parms->{name});
  my ($baseline);
  
  my $text = $_parms->{text};
  my $retval = $vbObj->setBaselineText($text) if ($vbObj);
  my $reply = ($retval) ? "OK" : "Error";
-->$reply
