## This file generated by InlineX::C2XS (version 0.25) using Inline::C (version 0.64)
package Math::GMPf::V;
use strict;
use warnings;

require Exporter;
*import = \&Exporter::import;
require DynaLoader;

our $VERSION = '0.44';
$VERSION = eval $VERSION;
Math::GMPf::V->DynaLoader::bootstrap($VERSION);

@Math::GMPf::V::EXPORT = ();
@Math::GMPf::V::EXPORT_OK = ();

sub dl_load_flags {0} # Prevent DynaLoader from complaining and croaking

1;
