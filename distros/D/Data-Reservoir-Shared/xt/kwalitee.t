use strict; use warnings; use Test::More;
plan skip_all => 'author test' unless $ENV{AUTHOR_TESTING};
eval { require Test::Kwalitee; Test::Kwalitee->import('kwalitee_ok'); 1 }
    or plan skip_all => 'Test::Kwalitee required';

# META.yml is generated by `make dist`, not committed to the source tree, so
# the meta-file-presence metric is a false negative when run against the working
# directory; the real tarball has it.
kwalitee_ok('-has_meta_yml');
done_testing;
