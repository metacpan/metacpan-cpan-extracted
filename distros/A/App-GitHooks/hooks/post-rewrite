#!/usr/bin/env perl

use strict;
use warnings;

use App::GitHooks;


=head1 NAME

post-rewrite - Call App::GitHooks for the post-rewrite hook.

=cut

App::GitHooks->run(
    name      => $0,
    arguments => \@ARGV,
);
