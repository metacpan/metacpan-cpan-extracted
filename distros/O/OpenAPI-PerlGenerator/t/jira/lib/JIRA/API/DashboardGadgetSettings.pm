package JIRA::API::DashboardGadgetSettings 0.01;
# DO NOT EDIT! This is an autogenerated file.
use 5.020;
use Moo 2;
use experimental 'signatures';
use Types::Standard qw(Str Bool Num Int Object ArrayRef);
use MooX::TypeTiny;

=head1 NAME

JIRA::API::DashboardGadgetSettings -

=head1 SYNOPSIS

  my $obj = JIRA::API::DashboardGadgetSettings->new();
  ...

=cut

sub as_hash( $self ) {
    return { $self->%* }
}

=head1 PROPERTIES

=head2 C<< color >>

The color of the gadget. Should be one of `blue`, `red`, `yellow`, `green`, `cyan`, `purple`, `gray`, or `white`.

=cut

has 'color' => (
    is       => 'ro',
    isa      => Str,
);

=head2 C<< ignoreUriAndModuleKeyValidation >>

Whether to ignore the validation of module key and URI. For example, when a gadget is created that is a part of an application that isn't installed.

=cut

has 'ignoreUriAndModuleKeyValidation' => (
    is       => 'ro',
);

=head2 C<< moduleKey >>

The module key of the gadget type. Can't be provided with `uri`.

=cut

has 'moduleKey' => (
    is       => 'ro',
    isa      => Str,
);

=head2 C<< position >>

The position of the gadget. When the gadget is placed into the position, other gadgets in the same column are moved down to accommodate it.

=cut

has 'position' => (
    is       => 'ro',
);

=head2 C<< title >>

The title of the gadget.

=cut

has 'title' => (
    is       => 'ro',
    isa      => Str,
);

=head2 C<< uri >>

The URI of the gadget type. Can't be provided with `moduleKey`.

=cut

has 'uri' => (
    is       => 'ro',
    isa      => Str,
);


1;
