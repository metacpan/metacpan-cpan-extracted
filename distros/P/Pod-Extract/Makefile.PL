# autogenerated by /home/rlauer/bin/make-cpan-dist.pl on Wed Apr  8 05:56:19 2026

use strict;
use warnings;

use ExtUtils::MakeMaker;
use File::ShareDir::Install;

if ( -d 'share' ) {
  install_share 'share';
}

WriteMakefile(
  NAME             => 'Pod::Extract',
  MIN_PERL_VERSION => '5.010',
  AUTHOR           => 'Rob Lauer <rclauer@gmail.com>',
  VERSION_FROM     => 'lib/Pod/Extract.pm',
  ABSTRACT         => 'Pod::Extract',
  LICENSE          => 'perl',
  PL_FILES         => {},
  EXE_FILES        => ['bin/podextract'],
  MAN3PODS         => { 'bin/podextract' => 'blib/man3/podextract.3' },
  PREREQ_PM        => {
    'CLI::Simple'            => '1.0.11',
    'CLI::Simple::Constants' => '1.0.11',
    'CLI::Simple::Utils'     => '1.0.11',
    'IO::Scalar'             => '2.113',
    'Module::Load'           => '0.36',
    'Readonly'               => '2.05'
  },
  BUILD_REQUIRES => {
    'ExtUtils::MakeMaker'     => '6.64',
    'File::ShareDir::Install' => 0,
  },
  CONFIGURE_REQUIRES => {
    'ExtUtils::MakeMaker'     => '6.64',
    'File::ShareDir::Install' => 0,
  },
  TEST_REQUIRES => {},
  META_MERGE    => {
    'meta-spec' => { 'version' => 2 },
    'prereqs'   => { 'runtime' => { 'recommends' => { 'Pod::Markdown' => '0' } } },
    'provides'  => {
      'Pod::Extract' => {
        'file'    => 'lib/Pod/Extract.pm',
        'version' => '1.0.0'
      }
    },
    'resources' => {
      'bugtracker' => {
        'mailto' => 'rclauer@gmail.com',
        'web'    => 'http://github.com/rlauer6/Pod-Extract/issues'
      },
      'homepage'   => 'http://github.com/rlauer6/Pod-Extract.git',
      'repository' => {
        'type' => 'git',
        'url'  => 'git://github.com/rlauer6/Pod-Extract.git',
        'web'  => 'http://github.com/rlauer6/Pod-Extract.git'
      }
    }
  }

);

package MY;

use File::ShareDir::Install;
use English qw(-no_match_vars);

sub postamble {
  my $self = shift;

  my @ret = File::ShareDir::Install::postamble($self);

  my $postamble = join "\n", @ret;

  if ( -e 'postamble' ) {

    local $RS = undef;

    open my $fh, '<', 'postamble'
      or die "could not open postamble\n";

    $postamble .= <$fh>;

    close $fh;
  }

  return $postamble;
}

1;
