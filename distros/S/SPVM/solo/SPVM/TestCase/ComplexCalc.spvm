class TestCase::ComplexCalc {
  sub add : Complocal_2d ($z1 : Complocal_2d, $z2 : Complocal_2d) {
    my $z3 : Complocal_2d;
    
    $z3->{re} = $z1->{re} + $z2->{re};
    $z3->{im} = $z1->{im} + $z2->{im};
    
    return $z3;
  }
}
