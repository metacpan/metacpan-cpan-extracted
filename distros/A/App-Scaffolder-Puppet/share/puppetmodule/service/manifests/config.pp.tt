#
# @summary Class to configure [% name %].
#
# @param ensure
#   Ensure that configuration is either <code>present</code> or
#   <code>absent</code>.
#
class [% name %]::config (
    Enum[present, absent] $ensure = 'present',
) inherits ::[% name %]::params {
}
