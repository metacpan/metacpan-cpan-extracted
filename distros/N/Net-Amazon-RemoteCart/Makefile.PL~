use 5.008;
use ExtUtils::MakeMaker;
# See lib/ExtUtils/MakeMaker.pm for details of how to influence
# the contents of the Makefile that is written.
WriteMakefile(
    'NAME'		=> 'Net::Amazon::RemoteCart',
    'VERSION_FROM'	=> 'lib/Net/Amazon/RemoteCart.pm', # finds $VERSION
    'PREREQ_PM'		=> {'Net::Amazon' =>0.18, }, # e.g., Module::Name => 1.1
    ($] >= 5.005 ?    ## Add these new keywords supported since 5.005
      (
       AUTHOR     => 'David Emery <dave@skiddlydee.com>') : ()),
);
