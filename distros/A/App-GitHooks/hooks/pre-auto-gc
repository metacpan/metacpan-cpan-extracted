#!/usr/bin/env perl

use strict;
use warnings;

use App::GitHooks;


=head1 NAME

pre-auto-gc - Call App::GitHooks for the pre-auto-gc hook.

=cut

App::GitHooks->run(
    name      => $0,
    arguments => \@ARGV,
);
