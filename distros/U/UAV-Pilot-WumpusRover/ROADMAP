* Send min/max radio settings packets
* Process video stream
* Fix packet parsing for when the data we have is longer than a single packet
    * Only process as much data as we need
* Document:
    * UAV::Pilot::WumpusRover::Video

