# -*- cperl -*-
# ABSTRACT: LaTeX Charactergroup object

=head1 SYNOPSYS

Represents a LaTeX Charactergroup as it can appear in a subscript

=cut

use strict;
use warnings;
package SpeL::Object::CharacterGroup;

use parent 'Exporter';
use Carp;

use Data::Dumper;

use SpeL::Object::CharacterElementList;

=method new()

We keep the default method, as the object is generated by the parser.

=cut

=method read( level )

returns a string with the spoken version of the node

=over 4

=item level: parsing level

=back

=cut

sub read {
  my $self = shift;
  my ( $level ) = @_;

  return $self->{MathUnit}->read( $level + 1 );
}

1;
