# This is a test file for Config.pm

# The callback to this field will lc the value.
LowerCase = VALUE

# The callback to this field will get get rid of the equals signs. Only the
# first equals sign should be considered the delimitting equals sign.
ExtraEquals = = None = Of = These

# This field tests read_config's whitespace trimming. The callback will get rid
# of the value's whitespace.
  ExtraWhitespace  =  Weird white space

# Since perl treats the string '0' as false, make sure we won't die if 0 is read
Zero = 0

# Test the 'File' parameter
TestFile = :-)
