# vim: tabstop=2 softtabstop=2 expandtab

---

sort-driver-file:
  class:
    name: "CLI::Driver::Exec"
    attr:
      required: ~
      optional: ~
      flags:
  method:
    name: sortDriverFile
    args:
      required:
        f: driverFile
      flags:
        write-to-stdout: writeStdout 