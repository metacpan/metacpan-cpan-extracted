function example (a, data, z) {
	this.a = a; this.data = data; this.z = z; }
example0 = new example(1, ['alpha',-1], 26 );
example1 = new example('', ['beta',-1.1], '' );
example2 = new example('', ['charlie',0.1], '' );
