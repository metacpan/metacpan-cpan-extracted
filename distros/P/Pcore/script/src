#!/usr/bin/env perl

package main v0.1.0;

use FindBin;
use if -f "$FindBin::RealBin/../share/dist.yaml" && -d "$FindBin::RealBin/../lib", lib => "$FindBin::RealBin/../lib";
use Pcore;

sub CLI ($self) {
    return 'Pcore::Util::Src';
}

__END__
=pod

=encoding utf8

=cut
