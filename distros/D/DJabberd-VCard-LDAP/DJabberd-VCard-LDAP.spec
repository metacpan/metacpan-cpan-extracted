Name:           DJabberd-VCard-LDAP
Version:        0.04
Release:        1%{?dist}
Summary:        LDAP vCard support for DJabberd
License:        Artistic
Group:          Development/Libraries
URL:            http://search.cpan.org/dist/DJabberd-VCard-LDAP/
Source0:        http://www.cpan.org/modules/by-module/DJabberd/DJabberd-VCard-LDAP-%{version}.tar.gz
BuildRoot:      %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)
BuildArch:      noarch
BuildRequires:  perl(DJabberd) >= 0.83
BuildRequires:  perl(DJabberd::Plugin::VCard)
BuildRequires:  perl(ExtUtils::MakeMaker)
BuildRequires:  perl(Net::LDAP)
Requires:       perl(DJabberd) >= 0.83
Requires:       perl(DJabberd::Plugin::VCard)
Requires:       perl(Net::LDAP)
Requires:       perl(:MODULE_COMPAT_%(eval "`%{__perl} -V:version`"; echo $version))

%description
LDAP vCard support for DJabberd

%prep
%setup -q

%build
%{__perl} Makefile.PL INSTALLDIRS=vendor
make %{?_smp_mflags}

%install
rm -rf $RPM_BUILD_ROOT

make pure_install PERL_INSTALL_ROOT=$RPM_BUILD_ROOT

find $RPM_BUILD_ROOT -type f -name .packlist -exec rm -f {} \;
find $RPM_BUILD_ROOT -depth -type d -exec rmdir {} 2>/dev/null \;

%{_fixperms} $RPM_BUILD_ROOT/*

%check
make test

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root,-)
%doc Changes README
%{perl_vendorlib}/*
%{_mandir}/man3/*

%changelog
* Thu Aug 23 2007 Edward Rudd 0.01-1
- Specfile autogenerated by cpanspec 1.73.
