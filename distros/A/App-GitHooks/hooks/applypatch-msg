#!/usr/bin/env perl

use strict;
use warnings;

use App::GitHooks;


=head1 NAME

applypatch-msg - Call App::GitHooks for the applypatch-msg hook.

=cut

App::GitHooks->run(
    name      => $0,
    arguments => \@ARGV,
);
