requires 'perl', '5.022001';

requires 'Class::Tiny';
requires 'File::Share';
requires 'Math::Random';
requires 'YAML::XS';

on configure => sub {
    requires 'Module::Build::Tiny';
};

on develop => sub {
    requires 'Perl::Tidy';
};

on test => sub {
    requires 'Test::More';
};
