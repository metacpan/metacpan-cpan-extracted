#!/usr/bin/env perl
# Create the example templates.

# Run with:   $0 <$xml-file>

use warnings;
use strict;

use lib 'lib';
use Business::CAMT  ();

use Log::Report mode => 'DEBUG';

use Data::Dumper;
$Data::Dumper::Indent = 1;

my $camt = Business::CAMT->new;

foreach my $version ($camt->knownVersions('053.001'))
{
}

exit 0;

