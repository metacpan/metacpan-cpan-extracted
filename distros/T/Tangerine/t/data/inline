use Inline;
use Inline C;
use Inline ('Java' => 'Config' => 'directory' => '/elsewhere');
use Inline 'CONFIG' => 'foo';
use Inline 'with';
use Inline 'with' => 'Alfa';
use Inline 'with' => qw(Bravo Charlie Delta);
use Inline PYTHON;
use Inline py;
use Inline 'Python';
