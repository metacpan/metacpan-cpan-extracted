Name:           perl-String-ProperCase-Surname
Version:        0.04
Release:        1%{?dist}
Summary:        Converts Surnames to Proper Case
License:        bsd
Group:          Development/Libraries
URL:            http://search.cpan.org/dist/String-ProperCase-Surname/
Source0:        http://www.cpan.org/modules/by-module/String/String-ProperCase-Surname-%{version}.tar.gz
BuildRoot:      %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)
BuildArch:      noarch
BuildRequires:  perl(Exporter)
BuildRequires:  perl(ExtUtils::MakeMaker)
BuildRequires:  perl(Test::Simple) >= 0.44
Requires:       perl(Exporter)
Requires:       perl(:MODULE_COMPAT_%(eval "`%{__perl} -V:version`"; echo $version))

%description
The package String::ProperCase::Surname is an Exporter that exports exactly
one function called ProperCase. The ProperCase function is for use on
Surnames which handles cases like O'Neal, O'Brien, McLean, etc.

%prep
%setup -q -n String-ProperCase-Surname-%{version}

%build
%{__perl} Makefile.PL INSTALLDIRS=vendor
make %{?_smp_mflags}

%install
rm -rf $RPM_BUILD_ROOT

make pure_install PERL_INSTALL_ROOT=$RPM_BUILD_ROOT

find $RPM_BUILD_ROOT -type f -name .packlist -exec rm -f {} \;
find $RPM_BUILD_ROOT -depth -type d -exec rmdir {} 2>/dev/null \;

%{_fixperms} $RPM_BUILD_ROOT/*

%check
make test

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root,-)
%doc Changes LICENSE README.md
%{perl_vendorlib}/*
%{_mandir}/man3/*
%{_mandir}/man1/*
%{_bindir}/*

%changelog
* Wed Aug 22 2012 Michael R. Davis (mrdvt92@yahoo.com) 0.03-1
- Specfile autogenerated by cpanspec 1.78.
