#!perl
use 5.008003;
use strict;
use warnings;
use Test::More;

unless ( $ENV{RELEASE_TESTING} ) {
    plan( skip_all => "Author tests not required for installation" );
}

# Ensure a recent version of Test::Pod::Coverage
my $min_tpc = 1.08;
eval "use Test::Pod::Coverage $min_tpc";
plan skip_all => "Test::Pod::Coverage $min_tpc required for testing POD coverage"
    if $@;

# Test::Pod::Coverage doesn't require a minimum Pod::Coverage version,
# but older versions don't recognize some common documentation styles
my $min_pc = 0.18;
eval "use Pod::Coverage $min_pc";
plan skip_all => "Pod::Coverage $min_pc required for testing POD coverage"
    if $@;

# Not all_pod_coverage_ok: the arch-installed File::Raw::Install::Files
# is generated by ExtUtils::Depends and has no POD to cover, and
# dl_load_flags is DynaLoader's hook rather than an interface of ours.
pod_coverage_ok('File::Raw', { trustme => [qr/^dl_load_flags$/] });

done_testing();
