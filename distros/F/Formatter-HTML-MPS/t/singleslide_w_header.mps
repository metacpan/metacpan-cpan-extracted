;; This is just a comment.
;; Configuration is done with lines like:
;; set VAR = VALUE

; set output_format = xhtml1.0_strict
; set title = Formatter::HTML::MPS
; set author = Vetle Roeim <vetler@gmail.com>

; newslide

# Formatter::HTML::MPS

