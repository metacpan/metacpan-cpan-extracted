# Comments added for further testing

# Make sure this definition is not used
# include = ${root.directory}/include2

include = ${root.directory}/include

# Make sure that values are not changed
include = ${root.directory}/include3

# And a value without any existing definitions is left partial
etc = ${system.directory}/etc
