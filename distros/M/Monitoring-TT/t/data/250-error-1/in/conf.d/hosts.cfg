[% IF host.type == 'linux' %]
define host {
    host_name     [% host.name %]
    address       [% host.address %]
    use           generic-host
    _TEST         [% host.nonexistant %]
}
[% END %]

﻿[% IF host.type == 'linux' %]
define host {
    host_name     [% host.name %]
    address       [% host.address %]
    use           generic-host
    _TEST         [% host.nonexistant %]
}
[% BROKEN SYNTAX %]

﻿[% IF host.type == 'linux' %]
define host {
    host_name     [% host.name %]
    address       [% host.address %]
    use           generic-host
    _TEST         [% host.nonexistant %]
}
[% END %]


