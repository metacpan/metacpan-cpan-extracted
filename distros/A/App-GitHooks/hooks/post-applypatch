#!/usr/bin/env perl

use strict;
use warnings;

use App::GitHooks;


=head1 NAME

post-applypatch - Call App::GitHooks for the post-applypatch hook.

=cut

App::GitHooks->run(
    name      => $0,
    arguments => \@ARGV,
);
