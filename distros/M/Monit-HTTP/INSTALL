To install:

        perl Makefile.PL
        make
        make test
        make install

Extra modules required for use:

        'LWP::UserAgent'
        'HTTP::Request'
        'Test::More'
        'XML::Bare'
        'YAML' =>
        'Error'
        'Test::Pod::Coverage'

This only works on Linux! The rest sux! :D
