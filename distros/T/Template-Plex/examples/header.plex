Header sub template:
@{[ do { 
	my $var =1;
	$package=__PACKAGE__;
	"Do string interp here whe calculation $var";
} ]}

This is the header sub template: title is  $title $surname;
and var is $var and package is $package
