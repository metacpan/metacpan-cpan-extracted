This is a set of client classes that perform PhyloTastic services. The classes
are designed such that they can both be used inside scripts and invoked from the
command line.

To install:

		$ sudo cpan Bio::PhyloTastic