#ifndef SLUG_CORE_H
#define SLUG_CORE_H

/*
 * slug_core.h — Pure C slug generation orchestration
 *
 * No Perl dependencies. Include this from plain C code or from slug.h
 * (the Perl bridge layer).
 */

#include "slug_transform.h"

#endif /* SLUG_CORE_H */
