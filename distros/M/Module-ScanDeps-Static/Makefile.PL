# autogenerated by /usr/local/libexec/make-cpan-dist.pl on Thu Nov 14 09:57:00 2024

use strict;
use warnings;

use ExtUtils::MakeMaker;
use File::ShareDir::Install;

if ( -d 'share' ) {
  install_share 'share';
}

WriteMakefile(
  NAME => 'Module::ScanDeps::Static',

  AUTHOR       => 'BIGFOOT <bigfoot@cpan.org>',
  VERSION_FROM => 'lib/Module/ScanDeps/Static.pm',
  ABSTRACT     => 'scan modules for dependencies',
  LICENSE      => 'perl',
  PL_FILES     => {},
  EXE_FILES    => ['bin/scandeps-static.pl'],
  PREREQ_PM    => {
    'CLI::Simple'           => '0.0.4',
    'Class::Accessor::Fast' => '0.51',
    'IO::Scalar'            => '2.111',
    'JSON'                  => '4.10',
    'Readonly'              => '2.05'
  },
  BUILD_REQUIRES => {
    'ExtUtils::MakeMaker'     => '6.64',
    'File::ShareDir::Install' => 0,
  },
  CONFIGURE_REQUIRES => {
    'ExtUtils::MakeMaker'     => '6.64',
    'File::ShareDir::Install' => 0,
  },
  TEST_REQUIRES => { 'Test::More' => '0' },
  META_MERGE    => {
    'meta-spec' => { 'version' => 2 },
    'provides'  => {
      'Module::ScanDeps::FindRequires' => {
        'file'    => 'lib/Module/ScanDeps/FindRequires.pm',
        'version' => '1.005'
      },
      'Module::ScanDeps::Static' => {
        'file'    => 'lib/Module/ScanDeps/Static.pm',
        'version' => '1.005'
      }
    }
  }

);

package MY;

use File::ShareDir::Install;
use English qw(-no_match_vars);

sub postamble {
  my $self = shift;

  my @ret = File::ShareDir::Install::postamble($self);

  my $postamble = join "\n", @ret;

  if ( -e 'postamble' ) {

    local $RS = undef;

    open my $fh, '<', 'postamble'
      or die "could not open postamble\n";

    $postamble .= <$fh>;

    close $fh;
  }

  return $postamble;
}

1;
