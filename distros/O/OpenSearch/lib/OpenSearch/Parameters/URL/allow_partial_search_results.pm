
  package OpenSearch::Parameters::URL::allow_partial_search_results;
  use Moose::Role;

  has "allow_partial_search_results" => (
    is => "rw",
    isa => "Bool",
    documentation => {
      encode_func => "encode_bool",
      required => undef,
    }
  );

  around "allow_partial_search_results" => sub {
    my $orig = shift;
    my $self = shift;

    if(@_) {
      $self->$orig(@_);
      return($self);
    }
    return($self->$orig);
  };

1;
