# autogenerated by /home/rlauer/bin/make-cpan-dist.pl on Thu Apr 30 16:59:35 2026

use strict;
use warnings;

use ExtUtils::MakeMaker;
use File::ShareDir::Install;

$File::ShareDir::Install::INCLUDE_DOTFILES = 1;

if ( -d 'share' ) {
  install_share 'share';
}

WriteMakefile(
  NAME             => 'Amazon::Signature4::Lite',
  MIN_PERL_VERSION => '5.010',
  AUTHOR           => 'Rob Lauer <rclauer@gmail.com>',
  VERSION_FROM     => 'lib/Amazon/Signature4/Lite.pm',
  ABSTRACT         => 'Amazon::Signature4::Lite',
  LICENSE          => 'perl',
  PL_FILES         => {},
  EXE_FILES        => [],
  MAN1PODS         => {},
  PREREQ_PM        => {
    'Digest::SHA' => '6.04',
    'URI::Escape' => '5.34'
  },
  BUILD_REQUIRES => {
    'ExtUtils::MakeMaker'     => '6.64',
    'File::ShareDir::Install' => 0,
  },
  CONFIGURE_REQUIRES => {
    'ExtUtils::MakeMaker'     => '6.64',
    'File::ShareDir::Install' => 0,
  },
  TEST_REQUIRES => { 'Amazon::Signature4::Lite' => '0' },
  META_MERGE    => {
    'meta-spec' => { 'version' => 2 },
    'provides'  => {
      'Amazon::Signature4::Lite' => {
        'file'    => 'lib/Amazon/Signature4/Lite.pm',
        'version' => '1.0.0'
      }
    },
    'resources' => {
      'bugtracker' => {
        'mailto' => 'rclauer@gmail.com',
        'web'    => 'https://github.com/rlauer6/Amazon-Signature4-Lite/issues'
      },
      'homepage'   => 'https://github.com/rlauer6/Amazon-Signature4-Lite',
      'repository' => {
        'type' => 'git',
        'url'  => 'git@github.com:rlauer6/Amazon-Signature4-Lite.git',
        'web'  => 'https://github.com/rlauer6/Amazon-Signature4-Lite'
      }
    }
  }

);

package MY;

use File::ShareDir::Install;
use English qw(-no_match_vars);

sub postamble {
  my $self = shift;

  my @ret = File::ShareDir::Install::postamble($self);

  my $postamble = join "\n", @ret;

  if ( -e 'postamble' ) {

    local $RS = undef;

    open my $fh, '<', 'postamble'
      or die "could not open postamble\n";

    $postamble .= <$fh>;

    close $fh;
  }

  return $postamble;
}

1;
